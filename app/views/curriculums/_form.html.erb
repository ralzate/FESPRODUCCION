<%= simple_form_for(@curriculum) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :usuario_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
